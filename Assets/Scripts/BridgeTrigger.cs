using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class BridgeTrigger : MonoBehaviour
{
    /*[SerializeField] private BridgeRotator bridgeRotator;

    public void OnTriggerEnter(Collider col)
    {
        Debug.Log("Trigger detected: " + col.name);

        if (col.gameObject.tag == "Player")
        {
            Debug.Log("Player entered the trigger! Rotating Bridge.");

            bridgeRotator.ActivateBridge();
        }
    }*/
}